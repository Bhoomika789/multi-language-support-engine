spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tool_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  security:
    user:
      name: none
      password: none

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: tool85demo@gmail.com
    password: kpzdwjvywjgwbeuw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

flyway:
  enabled: true
  locations: classpath:db/migration

server:
  port: 8080